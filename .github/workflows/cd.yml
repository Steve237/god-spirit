name: Pipeline-CD
on:
  push:
    branches: [ main ]
jobs:
  cd:
    runs-on: ubuntu-latest
    continue-on-error: false
    steps:
      - uses: actions/checkout@v2
      
      - name: Deploy Over SSH
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server: ftp://news-illuminati.com
          username: u395153934
          password: Espirito237!
          server-dir: /home/u395153934/public_html/
          script: 
            cd /home/u395153934/public_html/god-spirit
            git pull
            composer install
            symfony console d:m:m -n
            APP_ENV=prod APP_DEBUG=0 php bin/console cache:clear